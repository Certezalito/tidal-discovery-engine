# Scratch Directory

This directory is designated for temporary scripts, local spikes, debug experiments, and throwaway tests.

Files placed here are automatically ignored by Git (except this file and `.gitkeep`) to keep repository history and status clean.
